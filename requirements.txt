# Core scientific / data packages
cfgrib==0.9.15.1
eccodes==2.46.0        # install via: conda install -c conda-forge eccodes
geopandas              # install via: conda install -c conda-forge geopandas
numpy==2.3.5
openpyxl==3.1.5        # required by pandas for .xlsx support
pandas==3.0.1
requests==2.33.0
tzdata==2025.3
xarray==2026.2.0

# Build tool (developers only)
pyinstaller==6.19.0
